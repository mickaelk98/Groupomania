version: '3.8'
services:
  frontend:
    build: 
      context: ./frontend
      dockerfile: Dockerfile.prod
  backend:
    build: 
      context: ./backend
      dockerfile: Dockerfile.prod
    ports:
      - 5000:5000
  reverse-proxy:
    build: 
      context: ./reverse-proxy
      dockerfile: Dockerfile.prod
    ports:
      - 80:80